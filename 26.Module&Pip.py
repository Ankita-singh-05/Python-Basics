# modules
import useful_tools

print(useful_tools.roll_dice(10))

# PIP  -- PIP INSTALLS PACKAGES OR PIP INSTALLS PYTHON 
# Alternatively -- PREFERRED INSTALLER PROGRAM
# It refers to a package manager
# It is used to install the modules in python
# It is used to manage update, uninstall, install modules

# Installing Module -- go to command prompt
# eg- pip install python-docx
