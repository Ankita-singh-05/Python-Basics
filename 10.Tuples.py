# TUPLES
# Tuples are immutable 
# Means it cannot be changed or modified

coordinates = (4, 5)

#coordinates[1] = 10 # this will show an error because tuples are immutable

print(coordinates[1])

# Lists of tuples
coordinates2 = [(4, 5), (6, 7), (8, 9)]
print(coordinates2[1])

# 1.24
